import { AppTodoDirective } from './app-todo.directive';

describe('AppTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTodoDirective();
    expect(directive).toBeTruthy();
  });
});
